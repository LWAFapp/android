pluginManagement {
    repositories {
        google()
        jcenter()
        maven { url 'https://maven.google.com' } // Добавить эту строку
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        jcenter()
        maven { url 'https://maven.google.com' } // Добавить эту строку
    }
}
rootProject.name = "LWAF"
include ':app'
